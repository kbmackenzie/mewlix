takes "io.meows"

=^.x.^= fib(n)
    mew? (n <= 1)
        bring n
    meow meow
    bring fib(n - 1) + fib(n - 2)
meow meow

mew start = time()
meow(fib(30))
mew end = time()

meowf("Duration: %s", end - start)
