~( ^.x.^)> -----------------------------------------------
'Optimal Partition of String' Leetcode problem.
https://leetcode.com/problems/optimal-partition-of-string/
----------------------------------------------- <(^.x.^ )~

takes "hashset.meows"

=^.x.^= partitionString(str)
    mew chars = hashset()
    mew min = 1
    meowmeow(str)
        mew c = peek str
        mew? (not chars.contains(c))
            chars.add(c) 
            knock over str
            catnap
        meow meow
        paw at min
        chars.clear()
    meow meow
    bring min
meow meow
